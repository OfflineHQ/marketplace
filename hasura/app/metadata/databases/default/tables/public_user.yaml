table:
  name: user
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - address
        - email
        - emailVerified
      filter:
        id:
          _eq: X-Hasura-User-Id

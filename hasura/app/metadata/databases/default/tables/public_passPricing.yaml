table:
  name: passPricing
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - packId
        - eventPassId
        - amount
        - currency
      filter: {}
  - role: user
    permission:
      columns:
        - packId
        - eventPassId
        - amount
        - currency
      filter: {}

table:
  name: eventNftCollection
  schema: public
remote_relationships:
  - definition:
      to_remote_schema:
        lhs_fields:
          - eventId
        remote_field:
          event:
            arguments:
              where:
                id: $eventId
        remote_schema: Hygraph CMS Remote Schema
    name: event

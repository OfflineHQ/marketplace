table:
  name: eventPassPendingOrder
  schema: public
object_relationships:
  - name: account
    using:
      manual_configuration:
        column_mapping:
          accountId: id
        insertion_order: null
        remote_table:
          name: account
          schema: public
  - name: eventPassPricing
    using:
      manual_configuration:
        column_mapping:
          eventPassId: eventPassId
        insertion_order: null
        remote_table:
          name: eventPassPricing
          schema: public
remote_relationships:
  - definition:
      to_remote_schema:
        lhs_fields:
          - eventPassId
        remote_field:
          eventPass:
            arguments:
              where:
                id: $eventPassId
        remote_schema: Hygraph CMS Remote Schema
    name: eventPass
insert_permissions:
  - role: user
    permission:
      check: {}
      set:
        accountId: X-Hasura-User-Id
      columns:
        - eventPassId
        - quantity
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - eventPassId
        - accountId
        - quantity
        - created_at
      filter:
        accountId:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - quantity
      filter:
        accountId:
          _eq: X-Hasura-User-Id
      check: {}
delete_permissions:
  - role: user
    permission:
      filter:
        accountId:
          _eq: X-Hasura-User-Id

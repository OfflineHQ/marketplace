{
  "framework": "nextjs",
  "buildCommand": "bash -c 'set -e; pnpm nx run-many -t lint --all & pnpm nx run-many -t test --all & pnpm nx build back-office --prod; wait -n'",
  "outputDirectory": "../../dist/apps/back-office/.next",
  "installCommand": "pnpm install",
  "ignoreCommand": "bash -c '[[ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]] || [[ \"$VERCEL_GIT_COMMIT_REF\" = \"staging\" ]] || [[ \"$VERCEL_GIT_COMMIT_REF\" =~ ^pull/.* ]] && exit 0 || exit 1'"
}

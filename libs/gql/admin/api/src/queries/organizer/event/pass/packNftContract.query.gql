query GetPackNftContractFromPackId($packId: String) @cached {
  packNftContract(where: { packId: { _eq: $packId } }) {
    id
    chainId
    rewardsPerPack
    contractAddress
    eventPassIds
  }
}

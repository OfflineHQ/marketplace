# next-gql-user-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build next-gql-user-api` to build the library.

## Running unit tests

Run `nx test next-gql-user-api` to execute the unit tests via [Jest](https://jestjs.io).
